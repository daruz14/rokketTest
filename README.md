## Test RokketLab

Para iniciar este proyecto se debe hacer `npm install`

## Config

Se debe agregar en `next.config.js` el valor de la `app_id` para hacer uso de la API `https://dummyapi.io/`, que se debe generar u obtener a partir de la misma página.

## Run

Para levantar la app se debe ejecutar `npm run dev` o `npm run start`.